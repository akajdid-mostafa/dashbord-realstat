# Local development values (safe, non-secret).
# Full env file is NOT committed. Secrets (NEXT_PUBLIC_OPENCAGE_API_KEY) stay in .env.local / .env only.

NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_LOG_LEVEL=info

# Server-only (backend foundation). Never prefix with NEXT_PUBLIC_. Values live in gitignored .env / .env.local.
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
JWT_SECRET=
CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
